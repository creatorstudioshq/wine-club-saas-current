{
  "buildCommand": "pnpm build",
  "outputDirectory": "build",
  "functions": {
    "api/**/*.tsx": {
      "runtime": "@vercel/node"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}